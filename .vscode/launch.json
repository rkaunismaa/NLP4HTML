{

    // https://docs.scrapy.org/en/latest/topics/debug.html
    // The above did not work for me ... but the below stuff does!

    "version": "0.1.0",
    "configurations": [
        {
            "name": "Python: Launch Scrapy Spider",
            "type": "python",
            "request": "launch",
            "module": "scrapy",
            "args": [
                "crawl",
                "mostactive",
                "-o mostactive.jsonl"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceRoot}/yahooscraping"
        }
    ]
}